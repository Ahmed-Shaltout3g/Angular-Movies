import { SearchpeoplePipe } from './searchpeople.pipe';

describe('SearchpeoplePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchpeoplePipe();
    expect(pipe).toBeTruthy();
  });
});
